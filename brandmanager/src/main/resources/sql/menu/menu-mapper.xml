<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "https://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.oopssinsa.model.dao.MenuMapper">
<!--    <select id="insertProduct" resultType="String">-->
<!--        select * from test1-->
<!--    </select>-->
    <select id="login" resultType="accountDto">
        select
            *
        from
            account
        where
            id = #{id} and password = #{password}
    </select>
    <select id="showProduct" resultType="productDto">
        select
            *
        from
            product
        where
            brand_id = #{brandId}
    </select>
    <insert id="insertProduct">
        insert into
            product
        values (
        #{id}, #{brandId}, #{name}, #{size}, #{color}, #{categoryId}, #{volume}
        )
    </insert>
    <insert id="ibRequest">
        insert into
            ib_detail
        values (
        #{id}, #{manufactureId}, #{productId}, #{loginId}, #{quantity}, #{ibDate}, #{completionDate}, #{status}
        )
    </insert>
    <select id="findByUserID" resultType="ibDetailDto">
        select
            *
        from
            ib_detail
        where
            login_id = #{id}
    </select>
    <insert id="obRequest">
        insert into
            ob_request
        values(
        #{id}, #{productId}, #{loginId}, #{quantity}, #{recipientName}, #{address}, #{obDate}
        )
    </insert>

    <select id="findObDetail" resultType="obDto">
        select
        d.manufacture_date, d.ob_id, d.product_id, d.quantity, d.status, d.completion_date, d.tracking_number
        from
            ob_detail d left join ob_request r on d.ob_id = r.id
        where r.login_id = #{userId};
    </select>

    <select id="findAllStockDetail" resultType="stockListDto">
        select sd.*, p.name, p.size, p.color
        from stock_detail sd join product p on sd.product_id = p.id
        where p.brand_id = #{brandId};
    </select>

<!--    <resultMap id="productDtoMap" type="productDto">-->
<!--        &lt;!&ndash; id : PK컬럼 매핑용 &ndash;&gt;-->
<!--        <id column="id" property="id"/>-->
<!--        &lt;!&ndash; result : 그외 컬럼 매핑용 &ndash;&gt;-->
<!--        <result column="brand_id" property="brandId"/>-->
<!--        <result column="category_id" property="categoryId"/>-->
<!--        <result column="name" property="name"/>-->
<!--        <result column="size" property="size"/>-->
<!--        <result column="color" property="color"/>-->
<!--        <result column="volume" property="volume"/>-->
<!--        &lt;!&ndash; 1:N관계의 테이블 행 매핑 &ndash;&gt;-->
<!--        <collection property="stockDetailList" ofType="stockListDto">-->
<!--&lt;!&ndash;            <id column="order_code" property="orderCode"/>&ndash;&gt;-->
<!--&lt;!&ndash;            <id column="menu_code" property="menuCode"/>&ndash;&gt;-->
<!--            <result column="manufacture_date" property="manufactureDate"/>-->
<!--            <result column="product_id" property="productId"/>-->
<!--            <result column="quantity" property="quantity"/>-->
<!--            <result column="date" property="date"/>-->
<!--        </collection>-->
<!--    </resultMap>-->
</mapper>